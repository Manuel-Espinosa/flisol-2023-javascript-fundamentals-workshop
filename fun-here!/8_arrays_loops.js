const numbers = [1, 2, 3, 4, 5];
let sum = 0;

// Tu objetivo es recorrer el arreglo 'numbers' y sumar todos los números

// Completa el código debajo de esta línea

for (let i = 0; i < numbers.length; i++) {
    
}

// Completa el código encima de esta línea

console.log("La suma de los números es:", sum);
